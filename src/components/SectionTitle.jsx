const SectionTitle = ({ text }) => {
  return (
    <div>
      <h2 className='text-3xl font-medium tracking-wider mb-5 capitalize'>{text}</h2>
      <hr/>
      </div>
  );
};
export default SectionTitle;
